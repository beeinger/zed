[package]
name = "session_host"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
license = "GPL-3.0-or-later"
description = "Native agent, LLM, and event log hosted inside HeadlessProject"

[lints]
workspace = true

[lib]
path = "src/session_host.rs"
doctest = false

[dependencies]
session_protocol.workspace = true
